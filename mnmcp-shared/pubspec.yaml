name: mnmcp_shared
description: MnMCP shared components for all Flutter clients
version: 1.0.0

publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^2.4.0
  provider: ^6.1.1
  google_fonts: ^6.1.0
  window_manager: ^0.3.7
  logger: ^2.0.2
  uuid: ^4.2.1
  protobuf: ^3.1.0

flutter:
  uses-material-design: true
